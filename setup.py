from setuptools import setup, find_packages

setup(
    name="CodeForces-Tags-Prediction",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.8",
    install_requires=[
    ],
    author="Ouissal BOUTOUATOU",
    description="Codeforces problem tag prediction system",
)
